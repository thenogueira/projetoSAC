# Nome da aplica��o Spring Boot. Este nome � usado para identifica��o em logs, m�tricas, etc.
spring.application.name=projeto

# URL de conex�o com o banco de dados MySQL.
# O banco de dados est� rodando localmente (localhost) na porta 3306.
# O nome do banco de dados � "banco_de_usuarios".
spring.datasource.url=jdbc:mysql://localhost:3306/banco_de_usuarios

# Nome de usu�rio para a conex�o com o banco de dados.
# Neste caso, o usu�rio � "root", que � o usu�rio padr�o com privil�gios administrativos no MySQL.
spring.datasource.username=root

# Senha para o usu�rio especificado acima.
# Aqui, a senha � "mysql".
spring.datasource.password=mysql

# Dialeto SQL que o Hibernate (ORM usado pelo Spring Data JPA) deve usar para gerar as consultas SQL.
# Neste caso, est� configurado para usar o dialeto do MySQL, que � "org.hibernate.dialect.MySQLDialect".
# Isso garante que as consultas geradas sejam compat�veis com o MySQL.
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect